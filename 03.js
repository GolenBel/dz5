let i = 1;

while (i <= 20) {
    if (i % 2 === 1) {  // Проверяем, является ли число нечётным
        sum = sum + i;  // Если нечётное, добавляем к сумме
    }
    i = i + 1;          // Переходим к следующему числу
}

console.log(sum); // Выведет: 100
let sum = 0;
