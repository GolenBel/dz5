let i = 1;      // начинаем с 1
let sum = 0;    // переменная для суммы

while (i <= 20) {
    // Проверяем, нечётное ли число
    if (i % 2 === 1) {
        sum = sum + i; // или sum += i;
    }
    i++; // увеличиваем счётчик
}

// Результат: sum = 100