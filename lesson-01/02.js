let n = 1;          // Счётчик, начинаем с 1

while (n <= 10) {   // Условие: пока n меньше или равно 10
    factorial = factorial * n;  // Умножаем текущее значение факториала на n
    n = n + 1;      // Увеличиваем счётчик на 1
}

console.log(factorial); // Выведет: 3628800

let factorial = 1;  // Начинаем с 1, так как произведение начинается с 1
